// Placeholder for local `npm run dev` / `vite build` previews - the real
// file is generated at container startup (see
// docker-entrypoint.d/40-env-config.sh) from the API_BASE env var.
window.__ENV__ = {};
